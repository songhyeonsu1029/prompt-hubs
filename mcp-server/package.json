{
  "name": "prompthubs-mcp",
  "version": "0.2.0",
  "description": "Claude Code helpers for PromptHubs: legacy stdio MCP server + Stop hook auto-save (use the backend's HTTP transport for new setups)",
  "type": "module",
  "bin": {
    "prompthubs-mcp": "./server.js",
    "prompthubs-mcp-hook": "./auto-save-hook.js"
  },
  "files": [
    "server.js",
    "auto-save-hook.js",
    "README.md"
  ],
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  }
}